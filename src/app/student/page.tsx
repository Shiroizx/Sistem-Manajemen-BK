import { StudentDashboardClient } from './student-dashboard-client'

export default function StudentDashboardPage() {
  // This is now a client component page that handles NIS input
  return <StudentDashboardClient />
}

